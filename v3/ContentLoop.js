/**
 * in: 
 * targetID: container element's ID 
 * data: array of objects 
 * class component: as Comp 
 * register: bool (has event?)
 * out: a group of data sets applied to a template
 */

export function ContentLoop(targetID, data, register, Comp){
  // const target = document.getElementById(targetID);
  // for(let i=0; i<data.length; i++){
  //   // pass data to component
  //   let Component;
  //   Comp.type = "class" ? 
  //     Component = new Comp(data[i]) : 
  //     Component = Comp.component(data[i])
  //   const newElement = document.createElement(); 
  //   target.appendChild(newElement);
  //   // component applies dat to template and is
  //   // added to content
  //   if(register){ Comp.register(); };
  // }
  return content;
}